using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NaughtyAttributes;

public class Gimmick : MonoBehaviour
{
    
    public static float CatSpeed; // ‚Ë‚±‚Ì‘¬“x‚ðŽæ“¾
    [SerializeField, Label("”L‚Ì‰Á‘¬“x(float)")] private float accelerateSpeed = 1.5f; // ”L‚É—^‚¦‚é‰Á‘¬“x
    [Label("‰Á‘¬ƒMƒ~ƒbƒN")] public GameObject AccelerationGimmick;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
